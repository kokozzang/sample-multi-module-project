
plugins {
    id "idolpark.java-library-conventions"
    id 'idolpark.spring-common'
}

group = "com.example"
version = "1.0"

dependencies {

    api "org.springframework.boot:spring-boot-starter-batch"

}
